    mrmovq  x, %rbx     # Move content at address x in rbx x = 10
    mrmovq  y, %rax     # Move content at address y in rax y = 0
    addq    %rbx, %rax  # rax = rax + rbx y = 10 + 10
    rmmovq  %rax, y     # Move rax in the y location in memory
    halt

.pos 0x100
x: .quad 0x0a
y: .quad 0x0
